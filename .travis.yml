language: csharp
solution-name: dung.sln
dotnet: 3.1.202

env: TARGET=zip

os:
  - linux
  - windows
  - osx

matrix:
  include:
    - os: linux
      env: TARGET=docs


install: skip
script:
  - ./build.sh --target=$TARGET
